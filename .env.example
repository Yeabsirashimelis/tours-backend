# Application Environment
NODE_ENV=development
PORT=3000

# Database Configuration
# Replace <username>, <password>, <cluster>, and <dbname> with your MongoDB Atlas credentials
DATABASE=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT Configuration
# Generate a strong secret key (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90

# Email Configuration (Development - Mailtrap)
# Sign up at https://mailtrap.io for development email testing
EMAIL_USERNAME=your-mailtrap-username
EMAIL_PASSWORD=your-mailtrap-password
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=25

# Email Configuration (Production - Example: SendGrid)
# Uncomment and configure for production use
# EMAIL_FROM=noreply@yourdomain.com
# SENDGRID_USERNAME=apikey
# SENDGRID_PASSWORD=your-sendgrid-api-key

# Stripe Payment Configuration
# Get your keys from https://stripe.com/docs/keys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Optional: Frontend URL (for CORS)
# FRONTEND_URL=http://localhost:8080
